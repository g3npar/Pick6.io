# Pinned to the set verified working locally on Python 3.13.3 (see .python-version).
# nfl-data-py must stay at 0.3.2 — 0.3.3 added a numpy<2.0 pin, and the newest
# numpy under 2.0 (1.26.4) ships no wheels for 3.13+, so pip falls back to
# compiling numpy and pandas from source and the build fails.
nfl-data-py==0.3.2
nflreadpy==0.1.5
numpy==2.2.6          # transitive, but it's the package that broke the build
polars==1.41.1
psycopg2-binary==2.9.12
python-dotenv==1.2.2
pandas==2.3.2
requests==2.32.5
lxml==6.1.1
beautifulsoup4==4.14.3
